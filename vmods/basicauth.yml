# yaml-language-server: $schema=../schemas/vmod.schema.json
# vmod-basicauth (schema vmod/1, DESIGN.md).
schema: vmod/1
id: basicauth
upstream:
  git: https://git.gnu.org.ua/vmod-basicauth.git
  homepage: https://git.gnu.org.ua/vmod-basicauth
sources:
  head: main
  default:
    ref: v2.2
    version: "2.2"
package:
  summary: HTTP Basic Authentication VMOD for Varnish Cache
  description:
    - vmod-basicauth checks HTTP Basic Authentication credentials in VCL,
    - supporting password files and configurable authentication realms.
  license: GPL-3.0-or-later
  families:
    - varnish
