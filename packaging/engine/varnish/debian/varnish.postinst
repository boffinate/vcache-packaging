#!/bin/sh
set -e

if ! getent group varnish >/dev/null; then
    addgroup --system varnish
fi
if ! getent passwd varnish >/dev/null; then
    adduser --system --no-create-home --home /nonexistent --ingroup varnish varnish
fi
install -d -o varnish -g varnish /var/lib/varnish

#DEBHELPER#
