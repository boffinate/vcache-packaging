{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "vcache vmods/<id>.yml",
  "type": "object",
  "description": "One selected VMOD. Authority is 'python3 tools/matrix.py validate'; this schema is editor assistance only (DESIGN.md decision 11).",
  "additionalProperties": false,
  "required": [
    "id",
    "package",
    "schema",
    "sources",
    "upstream"
  ],
  "properties": {
    "schema": {
      "const": "vmod/1",
      "description": "Schema marker; must be 'vmod/1'."
    },
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "VMOD id. Must equal the filename stem \u2014 the validator checks that; this schema cannot."
    },
    "build": {
      "type": "string",
      "minLength": 1,
      "description": "Source build system. Defaults to autotools.",
      "enum": [
        "autotools",
        "cargo"
      ]
    },
    "upstream": {
      "type": "object",
      "description": "Where this VMOD lives upstream.",
      "additionalProperties": false,
      "required": [
        "git"
      ],
      "properties": {
        "git": {
          "type": "string",
          "minLength": 1,
          "description": "Canonical clone URL.",
          "format": "uri"
        },
        "homepage": {
          "type": "string",
          "minLength": 1,
          "description": "Project page, used in the package metadata.",
          "format": "uri"
        }
      }
    },
    "sources": {
      "type": "object",
      "description": "How this VMOD's source ref is chosen per engine.",
      "additionalProperties": false,
      "required": [
        "default",
        "head"
      ],
      "properties": {
        "head": {
          "type": "string",
          "minLength": 1,
          "description": "Branch used for the trunk lane's moving target."
        },
        "default": {
          "type": "object",
          "description": "The ref/version used for any engine without a by_series override.",
          "additionalProperties": false,
          "required": [
            "ref",
            "version"
          ],
          "properties": {
            "ref": {
              "type": "string",
              "minLength": 1,
              "description": "Git ref to build: a tag, branch, or commit. This is the pin."
            },
            "commit": {
              "type": "string",
              "minLength": 1,
              "description": "Expected full Git commit. Required by the validator for promoted package sources; the readable ref is checked out and must resolve to this immutable identity.",
              "pattern": "^[0-9a-f]{40}$"
            },
            "version": {
              "type": "string",
              "minLength": 1,
              "description": "Upstream version for package naming, quoted so it stays a string (e.g. \"1.7\", not 1.7)."
            }
          }
        },
        "by_series": {
          "type": "object",
          "minProperties": 1,
          "description": "Per-engine-series overrides, keyed by an engine's 'series' value. The validator additionally checks each key names a series some engine declares; this schema only checks shape.",
          "additionalProperties": false,
          "patternProperties": {
            "^[a-z0-9_.-]+$": {
              "type": "object",
              "description": "Override for this engine series.",
              "additionalProperties": false,
              "required": [
                "ref",
                "version"
              ],
              "properties": {
                "ref": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Git ref to build: a tag, branch, or commit. This is the pin."
                },
                "commit": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Expected full Git commit. Required by the validator for promoted package sources; the readable ref is checked out and must resolve to this immutable identity.",
                  "pattern": "^[0-9a-f]{40}$"
                },
                "version": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Upstream version for package naming, quoted so it stays a string (e.g. \"1.7\", not 1.7)."
                }
              }
            }
          }
        }
      }
    },
    "package": {
      "type": "object",
      "description": "What the generated .deb/.rpm says about itself.",
      "additionalProperties": false,
      "required": [
        "description",
        "license",
        "summary"
      ],
      "properties": {
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "One-line package summary (Debian Description / RPM Summary)."
        },
        "description": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Long description as a list of plain lines, one per line of prose. No '|' block scalars: the parser rejects them."
        },
        "license": {
          "type": "string",
          "minLength": 1,
          "description": "SPDX identifier, e.g. GPL-3.0-or-later, BSD-2-Clause."
        },
        "build_target": {
          "type": "string",
          "minLength": 1,
          "description": "Make target or target arguments used to build the installable VMOD. Defaults to 'all'; use this when upstream's default also builds an auxiliary program requiring an unavailable compiled engine tree."
        },
        "configure_args": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^--[A-Za-z0-9][A-Za-z0-9_.=-]*$"
          },
          "description": "Safe Autotools configure arguments for this VMOD's package build only, such as --enable-docs."
        },
        "build_deps": {
          "type": "object",
          "description": "Extra build dependencies, per packaging ecosystem.",
          "additionalProperties": false,
          "required": [],
          "properties": {
            "debian": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Extra Build-Depends beyond the common set."
            },
            "rpm": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "Extra BuildRequires beyond the common set."
            }
          }
        },
        "modules": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "A VCL import name: lowercase, digits and underscores."
          },
          "description": "VCL import names this VMOD ships. Defaults to [<id>]; required when the id is not a legal module name (e.g. varnish-modules)."
        },
        "artifacts": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]*\\.so$",
            "description": "A distinct basename-only release shared library ending in .so."
          },
          "description": "Cargo release shared libraries, in the same order as modules. Required for build: cargo."
        },
        "cargo_features": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]+$",
            "description": "A Cargo feature enabled for every build and test invocation."
          },
          "description": "Cargo features required to build the VMOD. Legal only for build: cargo."
        },
        "families": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "enum": [
              "vinyl",
              "varnish"
            ],
            "description": "An engine family, as engines.yml 'family' spells it."
          },
          "description": "Engine families this VMOD's build system supports. Gates package-mode expansion only; absent means no restriction, and compat cells are unaffected (DESIGN.md decision 13)."
        },
        "promoted": {
          "type": "string",
          "minLength": 1,
          "description": "Promotion gate (decision 15): package cells expand only when this is \"true\", so a new entry cannot block the release gate. Absent means not promoted; compat cells are unaffected.",
          "enum": [
            "true",
            "false"
          ]
        },
        "targets": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "A target id from engines.yml targets."
          },
          "description": "Targets packaging can work on at all (e.g. an x86_64-only VMOD lists only x86_64 targets). Gates package-mode expansion only; absent means every target (decision 15)."
        }
      }
    },
    "tests": {
      "type": "string",
      "minLength": 1,
      "description": "Opt in to the selected build system's upstream suite in compat mode.",
      "enum": [
        "make-check",
        "cargo-test"
      ]
    },
    "engine_source": {
      "type": "string",
      "minLength": 1,
      "description": "Declare that configure needs the engine source tree (VINYLSRC); the build scripts provision it (decision 14).",
      "enum": [
        "required"
      ]
    },
    "source_api_family": {
      "type": "string",
      "minLength": 1,
      "description": "API family named by this Autotools source tree. When it differs from the selected engine, the build harness normalizes the explicit family spellings before bootstrap.",
      "enum": [
        "vinyl",
        "varnish"
      ]
    }
  }
}
